/*
点差分
*/
//将u,v之间路径上的所有点权增加x
//统计以每个节点为根的树的节点的权值和，就是当前节点的最终权值
diff[u] += x, diff[v] += x, diff[lca] -= x, diff[fa[lca]] -= x;


/*
边差分
*/
//将u,v之间路径上的所有边权增加x
//以每条边两端深度较大的节点存储改变的差分数组
//统计以每个节点为根的树的节点的权值和，就是当前节点到父亲节点的边的最终权值
diff[u] += x, diff[v] += x, diff[lca] -= 2*x;
